/* this program is to swap two numbers without using third variable */
#include<stdio.h>
int main()
{
	int a, b;
	scanf("%d %d", &a, &b);
	printf("the swapped numbers are %d %d", b, a);
	return 0;
}
